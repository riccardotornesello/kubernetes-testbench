clusters:
  - name: "rome"
    runtime:
      type: "k3d"
    cni: "flannel"
    nodes: 2
    cache: true
    namespaces:
      - name: "test-namespace"
        deployments:
          - name: "nginx-deployment"
            replicas: 3
            pod_spec:
              containers:
                - name: "tester"
                  image: "riccardotornesello/hello-world-http-server:v2.1.0-tester"
                  ports:
                    - containerPort: 80
  - name: "milan"
    runtime:
      type: "kind"
    cni: "kindnet"
    nodes: 2
    cache: true
    namespaces:
      - name: "test-namespace"
        deployments:
          - name: "nginx-deployment"
            replicas: 3
            pod_spec:
              containers:
                - name: "tester"
                  image: "riccardotornesello/hello-world-http-server:v2.1.0-tester"
                  ports:
                    - containerPort: 80
